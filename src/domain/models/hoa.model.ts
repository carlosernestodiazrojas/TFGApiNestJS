

export class HoaModel {
    constructor(id: string, name: string, address: string) {
        this.id = id
        this.name = name
        this.address = address
    }
    id: string;
    address: string;
    name: string;
}